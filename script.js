function openProject(name) {
  alert("Project Selected: " + name + "\n\nYou can replace this with a modal or link later!");
}

function scrollToSection(id) {
  document.getElementById(id).scrollIntoView({ behavior: "smooth" });
}
